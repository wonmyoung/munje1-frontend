<template>
  <v-card color="grey lighten-4" flat tile>
    <v-toolbar dense>
      <v-toolbar-side-icon></v-toolbar-side-icon>
      <v-toolbar-title>LOGO</v-toolbar-title>
      <v-spacer></v-spacer>
      <template v-if="isLogin">
        <span>
          <span slot="title">{{ userInfo.username }}</span>
          <span @click="$store.dispatch('LOGOUT')">로그아웃</span>
        </span>
      </template>
      <template v-else>
        <a href="/login">로긴</a>
        <a @click="$router.push({ name: 'login' })">로그인</a>
        <a @click="$router.push({ name: 'home' })">hone</a>
        <a href="/signup">회원가입</a>
      </template>
      <v-btn icon>
        <v-icon>apps</v-icon>
      </v-btn>
    </v-toolbar>
  </v-card>
</template>

<script>
import { mapState, mapActions } from "vuex";

export default {
  name: "ContentActions",
  computed: {
    ...mapState(["userInfo", "isLogin"])
  },
  methods: {
    ...mapActions(["LOGOUT"])
  }
};
</script>
<style>
span {
  padding: 0 10px;
  margin: 0 10px;
  cursor: pointer;
}
</style>
